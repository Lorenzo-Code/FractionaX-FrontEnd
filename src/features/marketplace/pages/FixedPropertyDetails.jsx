// Create a temporary fixed file to demonstrate the solution
// This file should contain the corrected code that fixes the issue

// The key change is replacing lines 3353-5016 with:

                      })}\n                    </div>\n                  )}\n\n                  {/* Ownership History Tab */}

// This properly closes the overview tab's conditional content and removes all hardcoded car wash content.

// The user should then copy the corrected section into the main PropertyDetails.jsx file.